[tool.poetry]
name = "rincewind-logger"
version = "0.1.0"
description = "A module to handle in a standard way for all my cli python scripts."
authors = ["James Ainslie"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"

[tool.poetry.group.dev.dependencies]
pytest = "^8.1.1"
poetry = "^1.8.2"
wheel = "^0.42.0"
setuptools = "^69.1.1"
build = "^1.1.1"
twine = "^5.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
