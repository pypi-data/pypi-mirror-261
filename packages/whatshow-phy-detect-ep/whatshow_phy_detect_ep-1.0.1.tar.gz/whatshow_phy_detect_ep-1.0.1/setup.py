from setuptools import setup, find_packages

setup(
      name="whatshow_phy_detect_ep",
      version="1.0.1",
      packages=find_packages(),
      install_requires=[
          'numpy>=1.23.5'
      ],
);