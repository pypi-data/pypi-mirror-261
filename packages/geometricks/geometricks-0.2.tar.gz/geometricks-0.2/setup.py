from setuptools import setup, find_packages

setup(
    name='geometricks',
    version='0.2',
    packages=find_packages(),
    install_requires=[
        # No dependencies
    ],
    entry_points={
        'console_scripts': [
            # Entry point for command-line scripts (if any)
        ],
    },
)

