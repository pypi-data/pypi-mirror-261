from setuptools import setup

setup(
    name="Metrik",
    version="1.0",
    packages=["Metrik"],
    include_package_data=True,
    install_requires=[
        
        "pyodbc",
    ]
)
