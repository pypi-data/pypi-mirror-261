from setuptools import setup, find_packages

setup(
    name='sheljapm',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        'pyvisa',
        # Add your dependencies here if any
    ],
)