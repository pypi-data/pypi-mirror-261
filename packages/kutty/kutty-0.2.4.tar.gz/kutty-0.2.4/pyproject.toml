[tool.poetry]
name = "kutty"
version = "0.2.4"
description = ""
authors = ["kuttykishore <kishorekumar.mourougane@epita.fr>"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.7"
pandas = "^1.2"
scipy = "^1.5"
numpy = "^1.19"
plotly = "^4.9"
# Dependency on fbprophet removed or replaced with an alternative that doesn't require complex build environments


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
