# Sample configuration file for console.py. Copy this to your home dir, name
# it .asirc and edit.

asi_app_params = {
    'base_url': 'http://cos.alpha.sizl.org',
    'app_name': 'myapp',
    'app_password': 'secret',
}

asi_user_params = asi_app_params.copy()
asi_user_params.update({
    'username': 'joeuser',
    'password': 'hidden',
})
