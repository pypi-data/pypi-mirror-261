[tool.poetry]
name = "git-templates"
version = "0.1.0"
description = ""
authors = ["Octavio <octavio.delser@gmail.com>"]
readme = "README.md"
license = "MIT"

[tool.poetry.dependencies]
python = "^3.9"
pytest = "^8.0.2"


[tool.poetry.group.dev.dependencies]
pytest = "^8.0.2"
pytest-cov = "^4.1.0"
pytest-mock = "^3.12.0"
pre-commit = "^3.6.2"
safety = "^3.0.1"

[tool.poetry.scripts]
git-templates = "git_templates.main:main"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
