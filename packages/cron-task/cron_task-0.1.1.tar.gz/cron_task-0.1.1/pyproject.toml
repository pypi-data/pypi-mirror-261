[tool.poetry]
name = "cron-task"
version = "0.1.1"
description = "Script runner based on interval"
readme = "README.md"
homepage = "https://github.com/nsiregar/runner"
repository = "https://github.com/nsiregar/runner"
authors = ["Ngalim Siregar <ngalim.siregar@gmail.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
