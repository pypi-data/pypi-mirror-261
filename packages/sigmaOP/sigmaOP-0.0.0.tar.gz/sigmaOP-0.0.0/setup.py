# setup.py

from setuptools import setup, find_packages

setup(
    name='sigmaOP',
    version='0.0.0',
    packages=find_packages(),
    install_requires=[
        # Specify any dependencies your library may have
    ],
)
