from setuptools import setup, find_packages

setup(
    name='laifen',
    version='1.0.0',
    description='A simple package for addition within 100.',
    author='laifen',
    author_email='your.email@example.com',
    packages=find_packages(),
)
