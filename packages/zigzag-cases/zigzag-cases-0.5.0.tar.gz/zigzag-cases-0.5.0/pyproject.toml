[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "zigzag-cases"
authors = [
  {name = "Avinash R"}
]
version = "0.5.0"
description = "This is short description"
readme = "README.md"
