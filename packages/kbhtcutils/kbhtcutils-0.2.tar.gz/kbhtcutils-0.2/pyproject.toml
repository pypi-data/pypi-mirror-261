[build-system]
requires      = ["setuptools>=59.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "kbhtcutils"
version = "0.2"
description = "Utilities for testing and profiling the HTC-grid"
readme = "README.md"
authors = [{ name = "AWS", email = "aws-htc-grid@amazon.com"}]
# license = { file = "LICENSE" }
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent"
]

# keywords = ["feed", "reader", "tutorial"]
dependencies = ["boto3", "influxdb"]
# requires-python = ">=3.9"

#     [project.optional-dependencies]
#     build = ["build", "twine"]
#     dev   = ["black", "bumpver", "isort", "mypy", "pytest"]

#     [project.scripts]
#     realpython = "reader.__main__:main"

#     [project.urls]
#     repository    = "https://github.com/realpython/reader"
#     documentation = "https://realpython.com/pypi-publish-python-package/"


# [tool.bumpver]
# current_version = "1.1.2"
# version_pattern = "MAJOR.MINOR.PATCH"
# commit_message  = "bump version {old_version} -> {new_version}"
# commit          = true
# tag             = true
# push            = false

#     [tool.bumpver.file_patterns]
#     "pyproject.toml" = [
#         'current_version = "{version}"',
#         'version = "{version}"',
#     ]
#     "src/reader/__init__.py" = ["{version}"]
#     "src/reader/__main__.py" = ["- kb-reader v{version}"]

# [tool.isort]
# profile                   = "black"
# import_heading_stdlib     = "Standard library imports"
# import_heading_thirdparty = "Third party imports"
# import_heading_firstparty = "Reader imports"

# [tool.mypy]
# strict = true

#     [[tool.mypy.overrides]]
#     module                 = "feedparser"
#     ignore_missing_imports = true
